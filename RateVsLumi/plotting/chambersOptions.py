import ROOT
import numpy as np

class chamber():
    def __init__(self, name, color, marker, leglabel):
        self.name       = name
        self.color      = color
        self.marker     = marker
        self.legLabel   = leglabel

